name: hearable_device_sdk_sample_plugin
description: A new Flutter plugin project.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.nec.hearabledevicesdk.SamplePlugin.hearable_device_sdk_sample_plugin
        pluginClass: HearableDeviceSdkSamplePlugin
      ios:
        pluginClass: HearableDeviceSdkSamplePlugin